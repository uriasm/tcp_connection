# TCP Server Configuration

# Hostname or IP address where the server will bind
TCP_SERVER_HOST=127.0.0.1

# Port number for incoming TCP connections
TCP_SERVER_PORT=5000

# Timeout in seconds for socket operations (accept, recv, etc.)
TCP_SERVER_TIMEOUT=10